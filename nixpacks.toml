[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd backend && npm install"]  # Changed from npm ci to npm install

[phases.build]
cmds = ["cd backend && npm run build"]

[start]
cmd = "cd backend && npm run start:prod"